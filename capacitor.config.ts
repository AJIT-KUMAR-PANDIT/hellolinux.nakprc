import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hellolinux.nakprc',
  appName: 'helloLinux',
  webDir: 'build/client'
};

export default config;
